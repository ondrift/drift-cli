package main

import (
	"encoding/json"

	"drift-sdk"
)

func main() {
	drift.Run(func(req drift.Request) drift.Response {
		status, message, payload := {{FUNC}}()
		data, _ := json.Marshal(payload)
		return drift.Response{Status: status, Message: message, Payload: data}
	})
}
