mod drift;
mod {{SOURCE}};

fn main() {
    drift::run(|_req| {
        let (status, message, payload) = {{SOURCE}}::{{FUNC}}();
        serde_json::json!({
            "status": status,
            "message": message,
            "payload": payload
        })
    });
}
