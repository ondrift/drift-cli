mod drift;
mod {{SOURCE}};

fn main() {
    drift::run(|req| {
        let body = req.get("body").cloned().unwrap_or(serde_json::Value::Null);
        let (status, message, payload) = {{SOURCE}}::{{FUNC}}(&body);
        serde_json::json!({
            "status": status,
            "message": message,
            "payload": payload
        })
    });
}
